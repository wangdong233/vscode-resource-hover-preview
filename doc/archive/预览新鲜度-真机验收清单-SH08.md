# 预览新鲜度 — 真机验收清单(SH08 场景)

> 起源:0.5.17 前,同名覆盖图片后悬停预览仍旧图(直接打开=新图)。
> 修复:0.5.18/v0.28.10(server 强 ETag `size-mtimeMs` + `Cache-Control: no-cache` + 304/If-Range;overlay image 退出版本化缓存走 `fetchImageFresh` + `_imgMemo` 按 etag 版本化 memo)。
> 本文 = 出库条件 C1(4 路复审 G1):12 步真机验收 + 3 判别步,跑完在文末回写结论。
> 机械化回归锚:hooks/test-preview-freshness.mjs(③ 同名覆盖→200 新字节+新 etag)。

## 前置

- 版本确认:DevTools console `[mp-overlay] loaded v0.28.11`(或更新);未达则先 Cmd+Q 重启。
- 样例目录:`AIGC/特辑_产品介绍/vscode状态插件/flow-storyboard/film-v9/review/cards/`(任意图片目录均可)。

## 主验收(12 步)

| # | 操作 | 预期 |
|---|---|---|
| 1 | hover SH08.png | 旧图正常显示;移开 >200ms 浮窗关闭 |
| 2 | 用管线/任一方式**同名覆盖**重生成 SH08.png | 直接打开确认磁盘已是新图 |
| 3 | 回 VSCode re-hover SH08 | **必须新图**;移开再回仍新图 |
| 4 | DevTools(⌘⇧P→Toggle Developer Tools)→ Network 过滤 `preview` | re-hover 必出现 /preview 请求,size 列标 from disk/memory cache(304 协商表征)。**零请求 = 供旧回归** |
| 5 | 同会话静置 >5min 再 re-hover | 新图 |
| 6 | Cmd+Q 立即(<5min)重开 hover | 新图(证跨重启 HTTP 层已修) |
| 7 | Cmd+Q >5min 后重开 hover | 新图(**判别步③**,若仍旧图=存在模型外漏层,停手上报,禁叠补丁) |
| 8 | 预取回归:hover SH07 → 移到 SH08 | 出图 <50ms(Network 确认命中缓存) |
| 9 | 交互回归:SH08 滚轮放大→200ms 内移回(浮窗未关) | 缩放态保留零闪烁;关窗后再 hover→缩放复位 |
| 10 | 视频抽检:同名覆盖一个 mp4 → re-hover | 新内容可播、进度条 seek 正常(206/If-Range 未破坏) |
| 11 | 大 GIF(若可寻 ~10MB)re-hover 计时 | Network duration 记录回归幅度(预期 <100ms 级;memo 命中应显著更快) |
| 12 | font/3d 抽检 | 正常渲染(缓存保留路径未回归) |

## 判别步(残余边界,预期"仍旧图"=正确行为,非 bug)

| 判别 | 操作 | 预期 & 定性 |
|---|---|---|
| ① popup 可见期短路 | hover SH08 保持浮窗开着 → 覆盖文件 → **<200ms 内**鼠标从 popup 绕回同一行 | **仍旧图**(lastRenderedItem dedup 双层短路,任何 fetch 不发生)。语义=「取用时一致」非「展示中实时」;移开关窗再 hover 即新图 |
| ② 同尺寸覆盖 | 覆盖为**字节数完全相同**的图 | re-hover 新图(mtimeMs 参与指纹;闸门④已真跑证明)。极端角:同尺寸+同毫秒(或 `cp -p`/`rsync -a` 保留 mtime)覆盖=etag 碰撞永续供旧——业界(webpack 同款)接受 |
| ③ 跨会话长间隔 | 见主验收 #7 | 新图 |

## 文档化残余边界(设计取舍,非缺陷)

- **pin**:用户主动锁定=主动放弃新鲜(产品语义,豁免)。
- **font/3d**:会话内供旧至 LRU 驱逐/重启(稳定资产假设;3D probeSize 恒新 vs body 会话级=预存混合取舍,overlay 注释已认)。
- **失败方向**:若 Chromium 对 fetch 的 304 重组假设破,表现为 `r.ok=false` → 显式错误弹窗(**响亮可用性 bug,非静默供旧**)——遇此立即上报,升级为代码必修。

> ⚠️ **状态标注(2026-09-04 全项目双审 Y11)**:12 步真机验收**未执行**,新鲜度修复已随 0.5.18-0.5.24 迭代;Chromium 304 透明重组假设仍零 L3。须用户跑本清单回写结论,或显式签认风险自担——在此之前本文件的"出库条件 C1"名义未闭环。

## 结论回写

- 跑机日期/版本:
- 12 步结果(全过?列出未过项):
- 判别步 ①②③ 结果:
- 出库判定(零问题出库 / 有问题重入审查循环):
