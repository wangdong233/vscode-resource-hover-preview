{
  "name": "vscode-resource-hover-preview",
  "version": "0.3.0",
  "description": "VSCode 资源管理器悬停浮动预览（图片/视频/PDF/字体/音频/3D）",
  "type": "module",
  "bin": {
    "vscode-resource-hover-preview": "dist/patcher.js"
  },
  "scripts": {
    "build": "tsc",
    "build:lib": "mkdir -p resources/lib && cp node_modules/pdfjs-dist/build/pdf.min.mjs resources/lib/ && cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs resources/lib/ && esbuild entry-three.js --bundle --format=iife --outfile=resources/lib/mp-three.bundle.js",
    "companion:build": "tsc -p companion/tsconfig.json",
    "companion:package": "cd companion && rm -f *.vsix && mkdir -p resources/lib && cp ../resources/lib/* resources/lib/ && vsce package --no-dependencies",
    "prepublishOnly": "npm run build && npm run build:lib && npm run companion:build && npm run companion:package",
    "pretest": "npm run build && npm run companion:build",
    "patch": "tsx src/patcher.ts",
    "revert": "tsx src/patcher.ts --revert",
    "status": "tsx src/patcher.ts --status",
    "test": "node hooks/test-overlay-hash.mjs && node hooks/test-contract-sync.mjs && node hooks/test-patcher-io.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist/",
    "resources/",
    "resources/lib/",
    "hooks/",
    "companion/dist/",
    "companion/*.vsix",
    "*.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.84.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.28.1",
    "pdfjs-dist": "^6.2.108",
    "three": "^0.185.1",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
